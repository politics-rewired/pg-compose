describe("getter idempotency", () => {
  test("basic getter", async () => {});
});

describe("getter idempotency after changes", () => {
  test("change in return type", async () => {});
  test("change in table name", async () => {});
});
